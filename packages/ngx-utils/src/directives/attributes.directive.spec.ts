import { AttributesDirective } from './attributes.directive';

describe('AttributesDirective', () => {
  it('should create an instance', () => {
    let directive = new AttributesDirective();
    expect(directive).toBeTruthy();
  });
});
