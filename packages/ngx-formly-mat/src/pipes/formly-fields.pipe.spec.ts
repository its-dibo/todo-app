import { FormlyFieldsPipe } from './formly-fields.pipe';

describe('FormlyFieldsPipe', () => {
  it('create an instance', () => {
    let pipe = new FormlyFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
